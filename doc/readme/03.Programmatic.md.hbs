Programmatic API
-----

`{{pkg.name}}` also provides programmatic API.

Install as a local module,

```bash
$ npm install {{pkg.name}} --save-dev
```

then,

```javascript
{{{read 'example/example-program.js'}}}
```
